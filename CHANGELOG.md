# 更新日志

本项目采用 [语义化版本](https://semver.org/lang/zh-CN/) 管理版本，格式为 `主版本.次版本.修订版本`：

- **主版本**：不兼容的功能或使用方式变更。
- **次版本**：向后兼容的新功能。
- **修订版本**：向后兼容的问题修复与性能改进。

日期使用 `YYYY-MM-DD`。每次发布将版本标签（如 `v1.2.0`）指向对应的 Git 提交。

## [未发布]

### 性能优化
- 降低大纲滚动跟踪的重复布局计算，减少滚动时的额外动画。
- 降低本地文件监控、服务器文件监控和会话持久化的轮询频率；窗口重新获得焦点时仍会立即检查文件变化。
- 字数统计复用计算结果，避免重复扫描文本。

### 说明
- 外部文件修改的定时发现可能略有延迟；手动打开、重载和保存功能不受影响。
